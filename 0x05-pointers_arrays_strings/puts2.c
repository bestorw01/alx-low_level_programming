#include "main.h"

/**
* puts2 - Print all other char of a string
* @str: A pointer to an int that will be changed
*
* Return: Void
*/
void puts2(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if (a % 2 == 0)
		{
			_putchar(str[a]);
		}
		_putchar('\n');
}
